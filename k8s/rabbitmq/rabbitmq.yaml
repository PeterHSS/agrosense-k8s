apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-secret
  namespace: agrosense
type: Opaque
stringData:
  RABBITMQ_DEFAULT_USER: agrosense
  RABBITMQ_DEFAULT_PASS: agrosense
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rabbitmq
  namespace: agrosense
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rabbitmq
  template:
    metadata:
      labels:
        app: rabbitmq
    spec:
      containers:
        - name: rabbitmq
          image: rabbitmq:3-management
          ports:
            - containerPort: 5672
              name: amqp
            - containerPort: 15672
              name: management
          envFrom:
            - secretRef:
                name: rabbitmq-secret
---
apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: agrosense
spec:
  selector:
    app: rabbitmq
  ports:
    - name: amqp
      port: 5672
      targetPort: 5672
    - name: management
      port: 15672
      targetPort: 15672
